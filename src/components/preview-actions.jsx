import { PreviewControlsGrid } from './styled.js';

const PreviewActions = (props) => {
   

    return (
        <React.Fragment>        
            <PreviewControlsGrid />
        </React.Fragment>
    );
}
